apiVersion: v1
kind: Pod
metadata:
  name: nginx
  #namespace: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
  tolerations:
  - key: "project"
    operator: "Equal"
    value: "roboshop"
    effect: "NoExecute"  